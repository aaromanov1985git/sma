# ========================================
# Docker Environment Variables
# Скопируйте этот файл в .env для Docker Compose
# ========================================

# Database
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=waybill_verification
DB_PORT=5432

# Backend
NODE_ENV=production
PORT=3001
JWT_SECRET=change-this-secret-in-production-to-a-very-long-random-string
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost

# Frontend
FRONTEND_PORT=80
VITE_API_URL=http://localhost:3001/api

# pgAdmin (опционально)
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

